### Example .env for development (do NOT commit a real .env with secrets)
# Use this when running a local Postgres instance
# Example value for a local Docker container started using the commands in Server/README.md
### Local (dev) using host Postgres. When running via docker-compose use the
### service name `db` as host so the server container connects to the DB
### container correctly.
DATABASE_URL_LOCAL="postgres://app:pass@localhost:5432/trails"
DATABASE_URL_DOCKER="postgres://app:pass@db:5432/trails"

# Optional JWT secret used for signing tokens in development
JWT_SECRET="dev-secret-change-me"
